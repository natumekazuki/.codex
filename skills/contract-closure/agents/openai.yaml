interface:
  display_name: "Contract Closure"
  short_description: "複合不変条件を全経路へ展開し、直接検証と必要なレビューで閉じる"
  default_prompt: "Use $contract-closure to trace coupled invariants across sibling paths and failure states, verify them directly, and close only triggered review scope against an immutable commit."
