# natumekazuki/.codex v1.1.1

`v1.1.0`の`review-test-value`に対する互換修正です。

## 修正

- Git差分選択で、削除したtestのmetadataや閉じ行が隣接testと共通する場合に、削除済みrecordを隣接する存続testへ誤対応させないようにしました。
- 宣言開始行を削除・置換した旧recordは、同じ現行宣言に属する未変更の旧宣言行がある場合だけ位置による対応を認めます。対応先がなく、開始行の変更箇所にも現行宣言がなければ、旧recordを`DELETED.before`へ保持します。

## 互換性

- `v1.1.0`のCLI、構造化result、metadata v2、対応adapter、Git差分選択の既存契約を維持します。データ移行や新しい設定項目はありません。

## 検証

- Windows上のPython契約test 49件、多言語adapter回帰test 46件: 成功。検証時の実装・testファイルは、このタグの基点である`363c072da488e65ff58aa64947f44a428a9743c8`と一致します。
- 実環境への配置と新規Sessionでの動作確認は未実施です。
