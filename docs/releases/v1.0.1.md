# Codex v1.0.1

`v1.0.1`は、`v1.0.0`の`review-test-value`契約を維持した互換修正です。

## 修正と改善

### Git差分選択の削除・移設対応

- 固定base側でmetadata未付与のtestが削除・移設＋変更された場合も、`DELETED.before`へsourceと`metadata: null`を保持し、base側の`TEST_VALUE_MISSING`だけで差分審査を停止しないようにしました。
- Gitが移設＋変更を`D`＋`A`と判定した場合は削除側と追加側を独立したtransitionとして保持し、pathやsymbol名だけで`SURVIVED`を作らない契約を明記しました。
- 現行側のmetadata未付与・不正metadata、およびbase側の他のdiagnosticは引き続き審査を停止します。

## 互換性

- `v1.0.0`のCLI、構造化result、metadata v2、adapter、Git差分選択の既存契約を維持します。
- 削除前のmetadata未付与testについて、固定baseの`TEST_VALUE_MISSING`だけを歴史的証拠保持に切り替える互換修正です。現在の追加・変更testの診断は変わりません。
- データ移行や新しい設定項目はありません。

## 検証

- Python回帰test 49件、多言語adapter回帰test 41件: 成功。
- `extract_test_values.py`と`git_diff_selection.py`の構文検査: 成功。
- Skill validation: 成功。
- task baseからのGit差分抽出: diagnosticsなし。
- `general_luna` review: 完了、統合ブロッカーなし。
