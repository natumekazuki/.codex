# Codex v1.0.0

v1.0.0は、このrepositoryでGitタグによる互換性管理とリリースノート運用を開始する初回リリースです。

## 修正と改善

### review-test-valueのGit差分対応付け

- 同一の置換hunkで既存TypeScript testへmetadata v2を追加し、宣言をasync化した場合も、旧recordと新recordを`SURVIVED`として対応付けるようにしました。
- 新側hunk内の宣言候補が複数ある場合や、本文・位置による候補と競合する場合は、従来どおり`RECORD_TRANSITION_UNRESOLVED`として停止します。

## 互換性

- `v1.0.0`以前のタグなしcommitは開発途中として扱い、互換性を保証しません。
- `review-test-value`のCLI、構造化result、metadata v2、対応adapter、Git差分選択の契約を、次のリリースでも維持対象とします。
- データ移行や新しい設定項目の追加はありません。

## 検証

- 決定論的なPython回帰test・多言語adapter回帰test: 成功。
- `extract_test_values.py` と `git_diff_selection.py` の構文検査: 成功。
- 変更testの`review-test-value`抽出と`general_luna` review: 完了。
